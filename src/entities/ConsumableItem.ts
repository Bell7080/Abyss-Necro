// Placeholder consumable dropped by kills, separate from necromancy cards
// (hand) and relics (3-wave reward). No real use effect yet.
export interface ConsumableItem {
  id: string
  label: string
}
