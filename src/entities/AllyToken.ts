// Placeholder deployed defender until the 40-card roster/passives exist.
// Created when a hand card is placed on an empty board cell.
export interface AllyToken {
  id: string
  label: string
  hp: number
  maxHp: number
}
