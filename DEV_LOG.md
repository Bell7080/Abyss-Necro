# Dev Log — Abyss Necro

최신 항목이 위로 오도록 기록한다. 날짜별 요약만 남기고 장문 패치노트는 지양한다(상세 규칙 변경은 `CLAUDE.md`에 반영).

## 2026-07-03 (33) — 보드 hover 인스펙션(유닛 스탯/패시브·칸 버프)
- **hover 인스펙션**: 손패 카드를 들지 않은 상태에서 보드 칸/유닛에 커서를 올리면 우측 정보창에 정보가 뜬다 — 우선순위 앞 적 > 앞 아군 > (보스방)플레이어 > 칸 버프. 유닛은 공격/체력 칩 + 고유 패시브, 함정별이 깔린 칸은 "함정별 ×N · 진입 시 N 피해" 버프 라인이 붙는다. 빈 칸(버프 없음)은 표시 안 함. 조준 중(카드 선택)에는 선택 카드 정보가 우선하므로 hover 무시. 인접 칸 이동 시 깜빡임 방지용 60ms hide 디바운스.
- **인스펙터 확장**: `CardInspector.render(InspectorData)` 제네릭 추가(스탯 칩/패시브/버프 행) — 기존 손패 `show(card)`도 이를 통해 렌더. `CreatureDefinition.passive` 필드 신설(해파리/바다토끼에 flavor 패시브 — **표시 전용, 메커니즘 미구현**). `WaveSystem.getFrontEnemy/getEnemyAttack`, `DefenderSystem.getFrontAlly` 조회 API 추가.
- 검증: 적 hover(바다토끼·적·공격1·체력4/4·패시브), 플레이어 hover(넥슈·기본2/강화4/체력10/10), 이탈 시 숨김 Playwright 확인.

## 2026-07-03 (32) — 인스펙터 우측 이동·유물 독 z-index·적 중앙 라인 스폰
- **정보창(인스펙터) 우측으로**: `.card-inspector`를 좌측(left:0)에서 우측(right:0)으로 미러 — 진입 방향(translateX +18), 스크림 페이드 마스크(90deg, 보드 쪽=왼쪽 페이드), 본문 좌측 패딩 확대. 유물 독은 우하단 고정 유지하되 z-index를 인스펙터(95) 위(96)로 올려 조준 중에도 겹치지 않고 항상 보이게 했다.
- **적 중앙 라인 스폰**: `spawnWaveStaggered`가 랜덤 행 대신 항상 중앙 행(`CENTER_ROW=1`) 진입 칸에서 스폰하도록 변경 — 적은 가운데로 들어와 이동하며 상/하로 퍼진다.
- 검증: 인스펙터 우측 여백 0·유물 독(22,22)·웨이브1 적 (row 1, col 2) Playwright 확인.

## 2026-07-03 (31) — 소강 상점·에픽 시설 카드 5종·유물 독 분리
- **심연 상점**: 소강(체크포인트)마다 화면 중앙에서 반짝(✦ ✧ ✦)이며 펼쳐지는 상점 패널(`ShopOverlay`) — 랜덤 3슬롯(사령 45%/아이템 35%/에픽 20%), 가격 사령·아이템 ✦2 / 에픽 ✦6. 구매 시 별빛 차감(`CoinSystem.spend`) 후 카드가 버블로 손패에 꽂히고 슬롯은 sold 처리, 잔액 부족은 거절 쉐이크. "계속 나아가기"로 진행(보류된 웨이브 도착). 3번째 체크포인트는 기존 유물 3택 유지. `ProceedButton` 삭제.
- **에픽 시설 카드**(`EpicCardDefinitions`, 처치 시 6% 희귀 드롭 + 상점 판매): 함정별(그리드 칸에 영구 함정 — 진입 적 1피해, 중첩, 칸 우상단 진홍 ✦×n 마커) / 증가별(배치 아군 전체 공격력 +1 영구, 중첩) / 심연의 심장(최대체력 +2·즉시 회복) / 벼려진 저주(기본 공격 +1) / 과부하 문양(강화 공격 +2). 손패에서 보라 별 카드, 인스펙터에 "에픽 · 시설 카드" 설명, 사용 즉시 소모(함정별만 칸 지정 필요). 에픽은 합성 제외.
- **유물 독 분리**: 유물탭을 인스펙터에서 꺼내 항상 보이는 우측 하단 고정 독(`.relic-dock`)으로 복귀. 인스펙터(정보창)는 좌측 고정 그대로 — 제목/태그/설명만 남는다.
- 검증: 유물 독 우하단(22,22)·인스펙터 좌측 앵커, 24초에 첫 소강 상점 등장, 3슬롯 구성/가격, 구매(손패 4→5, sold 표시), 나가기 후 웨이브 4(2마리) 도착 Playwright 확인.

## 2026-07-03 (30) — 진영 발광·별자리 보드·아이템 카드·초반 레벨 디자인
- **진영별 은은한 발광**: 엔티티 카드의 하드 링 대신 넓은 소프트 헤일로로 — 적 붉은 발광 / 아군 푸른 발광 / 플레이어 보라 발광(`--card-ring`/`--card-glow` variant 값). 워터마크 실루엣 배경도 같은 진영색으로 재조정. 적 카드에도 크리처 이름 표기.
- **별자리 보드**: 칸을 "#" 별자리로 재해석 — 그리드 안쪽 경계선 2+2를 별빛 선(`board-constellation` SVG)으로 긋고 교차점 4개에 큰 별, 가장자리 8개에 작은 별을 두고 위상차 트윙클. 칸 자체는 테두리 없는 은은한 그림자 웅덩이로만 남아 "자리"임을 인지시킨다.
- **피격 점멸**: 모든 보드 카드(적/아군/플레이어)는 hp가 줄어드는 순간 붉게 빠르게 점멸(`.entity-card.is-hit`, hp 스냅샷 비교로 감지).
- **타원 그림자**: 모든 서 있는 카드 하단에 타원 접지 그림자(`board-figure::before`).
- **혜성 투사체**: `CurseMortar`를 단색 버블 혜성으로 재작성 — 선두 큰 오브 + 지연 간격으로 따라오는 점점 작아지는 꼬리 버블 5개가 같은 곡사 궤적을 그리고, 착탄 시 단색 조각 스플래시. 보라-마젠타 플랫 팔레트.
- **드롭 개편**: 처치 시 항상 카드 1장 — 사령 50% / 아이템 50%(1웨이브는 사령 확정). 아이템 카드(`ItemCardDefinitions`): "심연 파동"(필드 전체 1피해, `WaveSystem.damageAllEnemies`) / "치유 물결"(선택 칸 아군 3회복, `DefenderSystem.healCell`, 아군 없으면 소모 안 됨). 손패에서 청록 바이알 카드로 표시, 인스펙터에 사용형 설명. 아이템은 합성 대상에서 제외.
- **초반 레벨 디자인**: 웨이브 1/2 = 1마리, 3 = 2마리, 4~5 = 2마리, 6+ = 3마리(`enemyCountForWave`). 체크포인트는 3웨이브마다(1라운드=3웨이브). 필드 클리어 시 즉시 푸시 대신 3초 숨고르기(`CLEAR_PUSH_DELAY_MS`, 유효경과 북키핑으로 HUD 카운트다운도 3초 표시).
- **레이아웃 수정**: 보드 앵커 50%로 이동, 플레이어 시프트 -100px + 보스방 아군/적 오프셋 120/152로 재배치 — 250px 플레이어 카드가 화면 안에 온전히 들어오면서 중앙 카드들과 겹치지 않는다.
- 검증: 웨이브1 1마리·이름 표기·별 12개·피격 점멸·확정 사령 드롭·3초 후 웨이브2 1마리·플레이어 카드 rect(left 102) Playwright 확인.
- 미구현(다음): 소강 상점 탭(중앙 전개, 별빛으로 카드 구매), 에픽 시설 카드 5종(함정별/증가별/최대체력/일반공격/강화공격).

## 2026-07-03 (29) — 조준 모드(어둠+강조+0.3배속)·카드 인스펙터·합성 버튼·아이템 인벤 제거 등
- **배경 톤 다운**: bg 일러스트 위에 평평한 어둠 스크림(rgba 0.46)을 한 겹 더 깔아 무대 바닥으로 가라앉혔다.
- **플레이어 카드**: 250px로 확대, 이름을 타이틀급(30px 자간+시안·보라 헤일로)으로 강조, `--board-figure-shift-x -195px`로 본진 칸 밖 좌측까지 밀어 중앙 아군/적 카드와 겹치지 않게 했다(보스방 아군 오프셋 28px로 보정).
- **아이템 인벤토리 제거**: `ItemInventory`/`ItemSystem`/`ConsumablePool`/`ConsumableItem` 삭제, `dropItem` 플로우 제거 — 소모품은 추후 "손패의 일종"으로 재설계 예정. 우하단 side-panels 통째로 제거.
- **합성 버튼**: 자동 머지를 수동으로 전환 — 같은 기본 카드 3장이 모이면 손패 좌측에 보라 원형 "합성" 버튼이 반짝이며 등장(`MergeButton`, merge-twinkle 1.4s), 클릭 시 기존 젤리 머지 fx→tier-2 스왑. 애니메이션 동안 숨고 남은 트리플이 있으면 다시 켜진다.
- **조준 모드**: 손패 카드를 선택하는 동안 ① 화면 전체가 어두워지고(aim-dim, 보드만 z-index로 어둠 위로 부상) ② 배치 가능한 칸은 온기 링+글로우 beckon 펄스로 크게 강조 ③ 게임 전체 0.3배속 — `TickManager.setRate`(이동/전투/코스트)와 `WaveSystem.setTimeScale`(웨이브 푸시 카운트다운, 유효경과 북키핑으로 재무장) 동시 적용, 해제 시 원복.
- **카드 인스펙터**: 선택한 카드의 설명 레이어가 좌측에서 슬라이드 인 — Unmelting 거점 인스펙터 해부도(상단 풀블리드 일러스트→하단 근흑 그라데이션, 세로 중앙 앵커의 제목/디바이더/태그/설명) 좌측 미러 이식. 하단부에 유물탭을 상주시켜 허전함 보완(유물 획득 버블 목적지도 이 위치).
- 검증: 아이템 패널 부재, 조준 모드 어둠/인스펙터/유물탭/슬로모(HUD 카운트다운 4초간 ~1초 감소) Playwright 확인. 합성 버튼 클릭 경로는 테스트 봇 생존 한계로 미완 — 다음 레벨 디자인(웨이브당 1~2마리) 반영 후 재검증 예정.

## 2026-07-03 (28) — 기본 공격 "방금 떠난 칸" 유예 판정
- 모르타르 비행(520ms) 중 적이 다음 칸으로 이동하면 착탄이 빗나가던 문제. 적 토큰에 `lastCellIndex`/`lastMovedAt`을 기록하고, `WaveSystem.applyDamage`가 빈 칸에 착탄하면 950ms(슬라이드 0.85s + 여유) 안에 그 칸을 떠난 가장 최근 적을 **현재 칸에서** 대신 맞춘다. `DamageResult.cellIndex`가 실제 명중 칸을 담아 피해 숫자도 적을 따라간다(`Game.showHitNumber`). 궁극기 착탄도 같은 유예를 공유.
- Playwright 검증: 적이 칸을 떠난 직후 이전 칸 클릭 → hp 감소/처치 확인.

## 2026-07-03 (27) — 시작 전 스킬 시전 차단 + 시작 버튼 링리스 재손질
- **시작 전 스킬 시전 차단**: 인트로 오버레이가 버튼 밖에서 pointer-transparent라 보드 칸/궁극기 오르브 클릭이 시작 전에도 닿아 코스트가 소모되고 모르타르가 날아갔다. `Game.runStarted` 게이트를 `handleCellClick`/`castUltimate` 진입점에 걸어 "시작하기" 이전의 모든 시전을 차단(검증: 시작 전 클릭 후 코스트 10/모르타르 0 유지, 시작 후 정상 시전).
- **시작 버튼 링리스 재손질**: 인셋 위스퍼 링/내부 글로우를 전부 제거 — 은은한 radial 어둠 웅덩이 + 부드러운 드롭 섀도우 하나만 남기고, 발광하는 글자가 주인공. 호버 시 폰트 25→29px 확대 + 텍스트 글로우 상승으로 응답하고, `intro-breathe` 숨쉬기도 box-shadow 링이 아닌 text-shadow로 옮겼다.
- (참고) "보스칸 적이 보스를 안 때림" 리포트는 구버전 배포 캐시였음 — 최신 배포에서 정상 확인됨.

## 2026-07-03 (26) — 체크포인트 선스폰 수정 + 순차 스폰 + 트리플 젤리 머지 + 애니메이션 충돌 해소
- **체크포인트 선스폰 수정**: `handlePushTimeout`이 웨이브를 먼저 밀어넣고 나서 소강을 걸던 순서를 뒤집었다 — 체크포인트 차례가 되면 푸시 없이 바로 소강에 들어가고, 밀렸어야 할 웨이브는 `resumeFromCheckpoint()`(진행 버튼/유물 선택)에서 도착한다. 정비 중 화면에 적이 미리 서 있는 문제 해결.
- **순차 랜덤 스폰**: 웨이브 3기가 한 프레임에 나란히 슬라이드 인 하던 것을 `spawnWaveStaggered()`로 교체 — 랜덤 행 순서로 첫 기는 즉시, 나머지는 340~860ms 랜덤 간격으로 하나씩 등장(검증: 토큰 수 1→2→3). 대기 중 스폰 타임아웃은 `halt()`에서 취소된다.
- **애니메이션 충돌 해소(전진-후퇴 현상)**: 원인은 이동 슬라이드(0.85s transition)가 진행 중인 토큰에 런지 keyframe이 겹치면 keyframe이 목적지 기준으로 순간이동했다가 풀리며 후퇴처럼 보이는 것. 두 겹 방어 — ① `WaveSystem.tick`이 이동 직후가 아니라 900ms 뒤(슬라이드 완료 후, 1.3s 틱 안)에 클래시를 해소하고, ② `BoardRenderer`가 토큰 위치 변경 시각을 `dataset.movedAt`에 찍어 880ms 안에 움직인 토큰의 런지를 스킵한다(피해/버스트는 그대로).
- **트리플 젤리 머지**: 같은 크리처 기본 카드 3장이 손패에 모이면 Unmelting 머지 문법으로 합성 — 바깥 2장이 탄성 이징으로 가운데 카드에 자석처럼 붙고(0.4s), 붙는 순간 스쿼시-스트레치 젤리 + 밝기 펄스 + '✦ ✧ ✦' 시안 반짝이 터지며 바깥 2장이 녹아들고(0.54s), 마지막 비트에 모델이 교체되어 tier-2 카드 1장이 pop 등장. `HandSystem.findTriple/mergeTriple`(3장 중 하나라도 배치돼 사라졌으면 중단), `CardHand.playMergeFx`(표시 전용), Game이 카드 획득 시마다 감지·연쇄 처리. **tier-2는 진화체 로스터가 없는 동안의 플레이스홀더** — 보라 링/글로우로만 구분되고 능력치·아트 변화는 아직 없다(같은 크리처, 배치 시 일반 디펜더).
- Playwright 검증: 순차 스폰 카운트, 30초 파밍 만에 tier-2 카드 생성 확인.

## 2026-07-03 (25) — 스킬 아이콘·코스트 게이지·플레이어 피격/패배 + 적 전진 감속
- **스킬 아이콘 재디자인**: 기본 공격은 `CurseMortar`와 같은 실루엣의 "곡사로 날아가는 저주탄"(구체 + 곡선 트레일 3줄), 궁극기는 "전방위 파열"(중심 구체 + 장/단 광선 + 점선 궤도 링)으로 교체. 글리프 drop-shadow는 (23)에서 넣은 것 유지.
- **실시간 세로 코스트 게이지**: 오르브 왼쪽에 26×218px 캡슐형 게이지. `AbilitySystem.getSmoothCost()`가 다음 회복 틱까지의 진행도를 포함한 소수 코스트를 노출하고, `SkillBar`가 rAF로 매 프레임 fill 높이에 반영 — CSS transition 없이 움직임 자체가 데이터다. 10칸 구분선(정수 코스트), 수면 하이라이트 라인, 위로 흐르는 내부 shimmer, 아래 큰 발광 정수 숫자. 기존 "코스트 n/n" 텍스트 readout은 제거.
- **플레이어 피격/패배**: 신규 `PlayerSystem`(hp 10, 회복 없음). 플레이어 방에 도달한 적은 아군(디펜더)이 있으면 기존 클래시, 없으면 매 틱 선두 1기가 넥슈를 직접 공격(`WaveSystem.onPlayerHit` → Game이 라우팅: 피해 숫자 + engage 펄스 + 플레이어 카드 체력바 실시간 감소). hp 0이면 `TickManager.stop()` + `WaveSystem.halt()`(푸시 타이머 제거)로 월드를 정지하고 신규 `DefeatOverlay`가 인트로 역재생 톤으로 덮인다 — 붉은 "패배" 키커 + "심연에 잠기다" 타이틀 + 도달 웨이브 + "다시 도전하기"(페이지 리로드).
- **앰비언스 강화**: (23)의 비네틱/코스틱이 거의 안 보인다는 피드백으로 비네틱 최대 어둠 0.55→0.72, 코스틱 알파 0.05/0.04→0.10/0.07로 상향.
- **기본 볼드**: `FontManager.setPrimaryWeight(700)` 신설, 부팅 시 body 기본 굵기 볼드(단일 웨이트 폰트는 브라우저 합성 볼드).
- **적 전진 감속**: 이동 틱 1100→1300ms, 전진 확률 0.6→0.4(배회 상/하 각 0.2, 대기 0.2) — 새 스폰이 본진까지 기대 ~10초, 조준/배치할 여유가 생긴다.
- Playwright로 확인: 게이지가 궁극기 사용 직후 9%로 떨어졌다가 1.8초 만에 24%까지 실시간으로 차오르는 것, 적 누수 → 플레이어 피격 → 패배 오버레이 등장 → 웨이브 HUD 정지까지 전체 흐름.

## 2026-07-03 (24) — 폰트 전면 적용 + 별빛 패널 언멜팅화 + 인트로 버튼 확대 + 손패 2배·모임 부채꼴
- **폰트 미적용 원인 수정**: `<button>`은 폰트를 상속하지 않아 플레이어 카드(버튼 내부)·칸·오르브·손패가 UA 기본 폰트로 남아 있었다. `board.css`에 전역 `button { font: inherit }` 한 줄로 해결하고, `body`/`FloatingDamage`의 하드코딩 `system-ui`도 `var(--font-family-primary)` 폴백으로 교체해 FontManager가 지정한 폰트가 모든 텍스트에 닿게 했다.
- **별빛 패널(좌상단) 언멜팅 score-panel 문법으로 재구성**: 알약형 한 줄 카운터를 버리고 Unmelting `score-panel-total` 해부도를 그대로 이식 — 상단에 작은 자간 넓은 키커("✦ 별빛"), 아래에 큰 발광 tabular 숫자(30px/900), 뒤판은 상단에서 배어나오는 시안 radial 글로우뿐인 보더리스 워시. 획득 시 숫자 위로 '✦ ✧ ✦' 반짝 행이 솟는 Unmelting식 "딩" 연출 추가(기존 rise 비트 유지).
- **인트로 시작 버튼 확대**: 중앙에 자간 0.44em 플레이버 문구("심연이 그대를 기다린다")가 먼저 깔리고, 그 아래 대형 보더리스 버튼(25px/자간 0.34em, 인셋 보라 글로우)이 2.6s 주기로 느리게 숨쉬는 `intro-breathe` 발광을 갖는다 — 체크포인트 진행 버튼의 급한 펄스와 구분되는 "문턱 앞의 숨" 톤.
- **손패 2배 + 모임 부채꼴**: 카드 72×96→144×192(라벨 9→14px, 라운드 16px, 선택 부상 -30px). `fanTransform`을 개수 비례 폭에서 **고정 스텝**(74px 상한, 총폭 620px 캡) 방식으로 바꿔 몇 장 없을 때 겹쳐 모이게 했고, 부채 각도(7°×장수, 36° 상한)·호 깊이도 장수에 비례해 2장이면 거의 똑바로 선다. `getNextSlotPoint`가 같은 함수를 쓰므로 드롭 버블 조준도 자동 일치.
- Playwright로 확인: 플레이어 카드 이름·인트로 버튼의 computed font가 Griun Simsimche, 별빛 패널 키커/숫자 구조, 2장 손패가 겹친 부채꼴로 표시되는 것.

## 2026-07-03 (23) — 인게임 스타일 심연 세련화(테두리 제거 + 비네틱 + 물결 앰비언스)
- **Unmelting식 보더리스 패널 문법 적용**: Unmelting `score-panel`의 "뒤판은 거의 보이지 않는 어둠 워시, 콘텐츠가 주인공" 레시피를 전 HUD에 이식했다. 웨이브 HUD·코인 패널·유물/인벤토리 패널이 하드 1px 보더를 버리고 어두운 그라디언트 워시로 배경 어둠에 자연스럽게 녹아든다. 슬롯/체력바/엔티티 카드/손패 카드/보상 카드의 보더도 전부 `inset 0 0 0 1px` 위스퍼 링(box-shadow)으로 교체 — 상태 표현(적/아군/선택/호버)은 보더 색 대신 글로우와 링 강도로만 말한다. 엔티티 카드는 `--card-ring`/`--card-glow` 변수로 아군(온기)/적(냉기) 변형을 처리.
- **보드 칸 재손질**: 칸은 "바닥보다 살짝 깊은 어둠 웅덩이"로 — 보더 0, 그라디언트 워시 + 인셋 위스퍼 링. 호버(보라 글로우)/배치 타게팅(온기 링)/처치 펄스 모두 box-shadow 단일 채널로 정리.
- **스킬 오르브 재손질**: 보더리스 딥 글라스 구체(3-스톱 radial + 인셋 하이라이트 링 + 아래쪽 인셋 셰이드)로 재구성, 궁극기는 마젠타 톤 전용 글로우. 글리프에 drop-shadow를 줘 구체 위에서 떠 보이게 했고, 코스트 칩도 보더리스 링으로 통일.
- **신규 `AbyssAmbience`(`src/ui`)**: 화면 전체 고정 레이어(z-index 90 — 필드 위, 이펙트 220~240/오버레이 300~500 아래). ① 가장자리 비네틱이 HUD 주변에 자연스러운 어둠을 고이게 하고(패널이 보더 없이 녹아드는 근거), ② 오버사이즈 코스틱 시트 2장이 `screen` 블렌드 + transform 전용 드리프트(44s/67s 교차)로 물결에 일렁이는 빛을 아주 은은하게 깐다. `prefers-reduced-motion`에서 드리프트 정지.
- Playwright 스크린샷으로 확인: 보더리스 패널/칸이 배경에 녹아드는 톤, 호버 글로우, 배치 타게팅 온기 링, 앰비언스 레이어 2장+비네틱 DOM 존재, 궁극기→카드 드롭 전체 루프 정상.

## 2026-07-03 (22) — 폰트 매니저 도입 + 플레이어 이름 "넥슈" + 블라스트/틱 매니저로 이펙트·타이머 중앙화
- **버블 이펙트를 Unmelting 스타일로 정제**: `BubbleBurst`가 Unmelting `SquareBurst`의 스냅감 있는 이징(`cubic-bezier(0.18, 0.78, 0.28, 1)`)과 3단계 확대-안착 스케일 곡선, 바깥쪽으로 편향된 거리 분포(`rand(spread*0.35, spread)`), 개수 소폭 랜덤화를 그대로 가져와 단색 원형 조각이 더 정돈되고 경쾌하게 움직이도록 다듬었다. 사각형 전용인 회전은 원에는 의미가 없어 제외.
- 신규 `FontManager`(`src/ui/FontManager.ts`): Unmelting의 동명 클래스와 같은 패턴으로 `@font-face` 등록(`loadCustomFont`)과 전역 폰트 지정(`setPrimaryFamily`)을 한 곳에서 관리. 업로드된 `Griun Simsimche` 폰트를 `src/assets/fonts/griun_simsimche.woff2`로 넣고 `index.ts` 부팅 시점에 로드해 `body` 기본 폰트로 지정했다(개별 컴포넌트가 폰트 문자열을 직접 다루지 않게).
- 플레이어 카드 이름을 "네크로맨서"에서 "넥슈"로 변경(`BoardRenderer`).
- 신규 `TickManager`(`src/core/TickManager.ts`): `WaveSystem`(이동 틱)과 `AbilitySystem`(코스트 회복)이 각자 따로 갖고 있던 `window.setInterval`을 하나의 매니저로 모았다. 두 시스템은 `start(tickManager)`에서 자기 콜백/주기만 `register()`하고, 실제 인터벌 시작은 `Game.startRun()`이 `tickManager.start()`를 호출하는 한 지점으로 통일했다(전체 게임 시계를 한 번에 멈추는 `stop()`도 마련). 웨이브 푸시 타이머처럼 정지/재개로 지연이 동적인 것은 여전히 각 시스템 로컬 `setTimeout` 체인으로 남겨뒀다.
- 신규 `BlastManager`(`src/ui/effects/BlastManager.ts`): `Game.ts`에 흩어져 있던 `BubbleBurst`/`CoinDrop` 직접 호출 4곳(카드/아이템/유물 드롭, 코인 드롭, 클래시 팝)을 `travelDrop`/`coinDrop`/`clashBurst` 세 메서드로 정리했다. `Game.ts`는 이제 이펙트 라이브러리를 직접 import하지 않고 소스 rect·목적지 좌표·onArrive 콜백만 넘기며, 실제 상태 변경(`addCard`/`addItem`/`addCoins`/`addRelic`)은 그대로 콜백 안에 남아 있다.
- Playwright로 확인: 폰트가 실제로 `body`에 적용되고 `@font-face` 규칙이 주입되는 것, 플레이어 카드가 "넥슈"로 표시되는 것, 틱 매니저 경유로도 웨이브 이동·코스트 회복·즉시 다음 웨이브 푸시가 정상 동작하는 것, 블라스트 매니저 경유로 처치 시 카드 드롭 버블이 손패까지 도착해 실제 카드가 추가되는 것까지 전체 흐름.

## 2026-07-03 (21) — GitHub Pages 배포 재시도 버그 수정 + 보드/카드 2배 확대 + 손패 아트를 사령 후 형태로
- **배포 워크플로 수정**: `deploy.yml`에 "같은 run을 Re-run하면 100% 실패하는" 버그를 고쳤다. `upload-pages-artifact`가 내부적으로 `overwrite: false`로 업로드하기 때문에, 실패한 run을 재시도하면 이전 시도의 `github-pages` 아티팩트가 남아있는 채로 새 아티팩트가 또 생겨 `deploy-pages`가 "Multiple artifacts... found"로 확정 실패하는 것이 원인이었다. `actions: write` 권한을 추가하고, 업로드 전에 현재 run의 기존 `github-pages` 아티팩트를 `gh api` DELETE로 미리 지우는 스텝을 넣어 재시도해도 항상 성공하게 했다.
- **보드/카드 2배 확대**: `BoardRenderer`의 셀 픽셀 상수(`CELL_SIZE` 140→280, `CELL_GAP` 16→32, 역할 오프셋·스택 간격·클래시 돌진 거리 전부 비례 확대)와 `board.css`의 대응 치수(`.board-cell`/`.board-token` 140→280, `.board-player-cell` 160→320, `.board-layout` 그리드 트랙, `.entity-card` 계열 80~96px→160~190px, 카드 내부 이름/체력바/패딩)를 전부 2배 안팎으로 키웠다. 큰 지오메트리에 맞춰 `perspective`(950→1900)·`translateZ`(16→32)도 비례 조정해 기울기 강도는 그대로 유지. 커진 보드가 우측 유물/인벤토리 패널과 겹쳐서 `.board-mount`의 수평 앵커(`left` 54%→46%)도 같이 조정했다.
- **손패 카드 아트를 사령 후(after)로 변경**: `CardHand`가 그동안 `creature.enemyArt`(적 상태)를 보여주고 있던 것을 `creature.allyArt`(사령된 형태)로 바꿔, 손에 든 카드가 배치했을 때 실제로 나올 아군 모습을 미리 보여주게 했다.
- Playwright로 확인: 1920×1080에서 확대된 보드가 패널과 겹치지 않는 것, 손패 카드가 사령된 캐릭터 일러스트로 표시되는 것, 배치 후 보드 위 아군 카드도 커진 크기로 정상 렌더링되는 것.

## 2026-07-03 (20) — 첫 실제 원화 적용(해파리/바다토끼/플레이어/배경/아이콘)
- 신규 `CreatureDefinitions.ts`: 40종 로스터의 첫 2종(해파리 `en_001`/`al_001`, 바다토끼 `en_002`/`al_002`)을 before(적)/after(사령된 아군) 원화 쌍으로 등록. `EnemyToken`/`AllyToken`/`HandCard`가 모두 `creatureId`를 들고 다니며 스폰→처치→카드 드롭→배치 전 구간에서 같은 크리처 정체성이 유지된다(`WaveSystem`이 스폰 시 무작위 배정, 처치 시 `EncounterResult.creatureId`로 전달, `Game`이 카드 생성/배치에 그대로 사용).
- `EntityCard`/`BoardRenderer`/`CardHand`: `imageUrl`이 있으면 기존 flat SVG 워터마크 대신 실제 원화를 풀 프레임 `object-fit: cover`로 렌더링하도록 확장(`has-image` 변형 클래스로 배경 그라디언트/워터마크를 끔). 아직 원화가 없는 나머지 적 종류는 기존 placeholder 아이콘을 그대로 사용해 혼재 가능.
- 플레이어 카드는 `player_001.webp`로 교체(`Icons.skullCrown()` 워터마크는 더 이상 쓰이지 않아 제거).
- 배경(`bg_001.webp`)을 `body` 배경으로 적용, 하단 비네틱만 얇게 얹어 HUD 가독성 유지.
- 아이콘(`icon_001.png`, 1024×1024)은 `src/assets/sprites`가 아니라 실제 쓰이는 자리로 이동: `build/icon.png`(electron-builder `package.json`의 `build.icon`, Windows .ico 자동 생성용)와 `src/public/favicon.png`(브라우저 탭 favicon, `index.html`에 `<link rel="icon">` 추가) 두 곳에 복사.
- Playwright로 확인: 웨이브 스폰 시 해파리/바다토끼 카드가 무작위로 섞여 등장, 처치 후 손패 카드에 같은 크리처의 적(before) 원화가 표시, 배치 시 보드에 사령된(after) 원화로 교체되는 전체 흐름.

## 2026-07-03 (19) — 인트로 비네틱 부팅 화면 + 시작하기 버튼 + 에셋 폴더 스캐폴딩
- 신규 `IntroOverlay`: 게임 진입 시 화면 전체를 덮는 어두운 비네틱(`radial-gradient` 다크 스크림)이 표시되고 2.4초에 걸쳐 서서히 밝아지며 그 아래 이미 렌더된(비어있는) 보드가 드러난다. 밝아진 직후 중앙에 "시작하기" 버튼이 페이드인되며 `.proceed-button`과 같은 발광 펄스 애니메이션을 공유한다.
- `WaveSystem`/`AbilitySystem`의 초기화 로직(웨이브 스폰·이동 틱·푸시 타이머·코스트 회복 타이머)을 생성자에서 분리해 각각 `start()` 메서드로 옮기고, `Game.startRun()`이 "시작하기" 클릭 시에만 이 둘을 호출하게 했다 — 그 전까지는 보드가 비어 있고 웨이브 HUD도 "웨이브 1 - 00:30"에서 멈춰 카운트다운이 돌지 않는다.
- `src/assets/sprites/{enemies,allies,backgrounds}/` 폴더를 `.gitkeep`과 함께 미리 만들어 실제 원화(webp) 투입 자리를 마련했다(적/사령된 아군/배경 3종 — 추가 카테고리는 필요 시 확장).
- Playwright로 확인: 로드 직후 완전히 어두운 화면 → 서서히 밝아짐 → 버튼 등장까지 비네틱 오파시티 단계별 변화, 시작 전 보드에 적 토큰 0개·웨이브 HUD 정지, 버튼 클릭 후 오버레이 제거+적 3기 스폰+HUD 정상 작동 전부 확인.

## 2026-07-03 (18) — 코인 착지 후 소폭 딜레이
- (17)번에서 코인이 착지하자마자 바로 패널로 블라스트되어 너무 즉각적으로 느껴졌다는 피드백에 따라, `Game.ts`에 `COIN_LAND_PAUSE_MS`(260ms)를 두어 `CoinDrop.onLand` 이후 `BubbleBurst.travelTo` 시작을 그만큼 지연시켰다 — 코인이 땅에 잠깐 머물렀다가 패널로 날아가는 것처럼 보이게.
- Playwright의 `MutationObserver`로 코인 스프라이트 제거 시점과 첫 버블 조각 삽입 시점을 직접 타임스탬프 찍어 그 간격이 정확히 ~260ms인 것을 확인.

## 2026-07-03 (17) — 처치 시 코인 확정 드롭 + 좌상단 코인 패널
- 신규 `CoinSystem`(단순 누적 카운터)과 좌상단 `CoinPanel`을 추가. 아이콘은 Unmelting의 불빛/화폐 공용 "네 꼭짓점 반짝 다이아" 패스를 그대로 가져와 `Icons.coinSparkle()`로 재구현하고, 색만 심연 톤 청록(`#7fe8ff`)으로 재해석했다. 패널도 Unmelting 좌상단 패널 문법(둥근 알약형 다크 글라스 패널)을 그대로 따르되 심연 팔레트로 재해석.
- `WaveSystem.EncounterResult`에 `dropCoin`(항상 true)을 추가해 적 처치 시 100% 확정으로 코인 1개가 드롭되게 했다.
- 신규 `CoinDrop` 이펙트: `CurseMortar`와 같은 WAAPI 포물선 기법을 재사용하되, 처치 지점 바로 위(130px)에서 짧게 곡사포처럼 낙하해 그 자리에 착지하고(약간의 좌우 드리프트로 "던져진 포탄" 느낌), 착지 순간 얕은 청록 임팩트 링을 띄운다.
- 착지 직후 그 지점에서 기존 `BubbleBurst.travelTo`(카드/아이템/유물 배달에 이미 쓰던 심연 버블 반짝임 비주얼)를 그대로 재사용해 코인 패널까지 날아가고, 도착 시 `CoinPanel.pulse()`(살짝 떠올랐다 내려오는 rise 애니메이션 + 숫자 발광)와 함께 카운트가 올라간다.
- 코인 사용처(소강 정비 단계 상점 등)는 이번 범위에 포함하지 않고 누적만 구현 — 추후 세션에서 이어서 구현 예정.
- Playwright로 확인: 처치 즉시 코인 드롭 스프라이트가 착지→버블 여행→패널 카운트 0→1 증가까지 실제 동작.

## 2026-07-03 (16) — 소환수 실험 롤백 + 배치 디펜더 체력 하향
- (15)번 소환수 실험을 플레이해보고 "별로"라는 판단이 나와, 기본 공격/스킬을 다시 즉발 딜(`CurseMortar` + `WaveSystem.applyDamage` + `FloatingDamage`)로 되돌렸다. 기본 공격 코스트는 2로 유지(궁극기 10 그대로), `Game.castBasicAttack`/`castUltimate`가 다시 클릭한 칸(또는 전체 생존 칸)에 곡선 투사체를 쏘고 착탄 시 데미지 숫자를 띄운다.
- `DefenderSystem.summon`/`stepSummons`/`getAttack`, `WaveSystem`의 관련 훅, `AllyToken.attack`/`movesLeft` 필드는 삭제하지 않고 그대로 남겨뒀다(어느 쪽 능력도 더 이상 호출하지 않지만, 추후 다른 스킬/유물/보스 패턴에 재사용 가능하도록 보존).
- 배치형 디펜더(손패 카드) 기본 체력을 `ALLY_BASE_HP` 6 → **3**으로 낮췄다. Playwright로 새 카드를 적이 있는 칸에 배치해 100%→67%→33%→소멸까지 정확히 3회 피격만에 죽는 것으로 확인.

## 2026-07-03 (15) — 기본/궁극기를 즉발 딜에서 소환수 소환으로 전환
- 기본 공격/스킬의 정체성을 "즉발 데미지"에서 "보스 칸 옆에서 나타나 필드를 배회하는 임시 소환수"로 바꿨다. `DefenderSystem.summon(label, hp, attack, moves)`이 그리드 0열(보스 칸과 맞닿은 열)의 무작위 행에 소환수를 등록하고, `DefenderSystem.stepSummons()`가 `WaveSystem.tick()` 안에서 적 이동과 같은 틱에 한 칸씩(60% 전진/20%·15% 상하 배회/5% 정지, 전진은 그리드 끝에서 막힘) 이동시키며 실제 이동 횟수(`movesLeft`)가 0이 되면 그 자리에서 소멸한다.
- 소환수는 배치형 디펜더와 동일한 칸 배열에 들어가 기존 클래시 로직을 그대로 타므로 별도 전투 코드가 필요 없다 — 다만 아군 공격력이 항상 고정값이던 것을 `DefenderSystem.getAttack()`(소환수는 자기 attack 값, 배치 카드는 기존 기본값 2)으로 바꿔 소환수마다 공격력이 달라지게 했다.
- 기본 공격(코스트 1→**2**): "멸치 소환수"(hp2/공격1/이동 12칸). 스킬(코스트 10, 변경 없음): "정예 소환수"(hp8/공격4/이동 30칸). `SkillBar`의 코스트 라벨을 `AbilitySystem`의 실제 상수(`BASIC_ATTACK_COST`/`ULTIMATE_COST`)를 참조하도록 바꿔 하드코딩된 "1"이 실제 값과 어긋나지 않게 했다.
- 기존 즉발 투사체(`CurseMortar`)·데미지 숫자(`FloatingDamage`)·`WaveSystem.applyDamage`/`getAliveCellIndices`는 이번 요청으로 더 이상 기본/궁극기에 연결되지 않지만, 나중에 다른 곳(다른 스킬·유물·보스 패턴)에 재사용할 수 있도록 삭제하지 않고 코드 그대로 보존했다.
- Playwright로 확인: 칸 클릭 시 코스트 10→8(-2)과 함께 "멸치 소환수" 카드가 보스 칸 옆 0열에 등장, 약 12틱 뒤 자연 소멸. 코스트를 10까지 채운 뒤 스킬 아이콘 클릭 시 코스트 10→0, "정예 소환수" 카드가 동일한 위치에 풀피로 등장하는 것.

## 2026-07-03 (14) — 클래시 롤백 버그 수정 + 정비 중 공격 차단 + 카드 크기 확대
- **클래시 롤백 버그 수정**: `WaveSystem.tick()`이 `resolveClashes()`(클래시 판정 + `onClash` 발사)를 `emitChange()`(화면 위치 동기화)보다 먼저 호출해서, 적이 아군이 있는 칸으로 걸어 들어오며 같은 틱에 바로 클래시가 시작되는 경우 `BoardRenderer.playClashFx`가 아직 갱신 안 된 "이전 칸" 좌표 기준으로 돌진 애니메이션을 재생하고, 그 직후 `emitChange()`가 올바른 좌표로 갱신되며 기존 슬라이드 트랜지션이 다시 실행돼 "부딪히고 나서 뒷칸으로 밀려났다 되돌아오는" 것처럼 보이는 롤백 버그가 있었다. `tick()` 안에서 `emitChange()`를 `resolveClashes()`보다 먼저 호출하도록 순서만 바꿔 토큰 좌표가 항상 최신 상태에서 클래시 이펙트가 재생되게 고쳤다.
- **정비 중 공격 차단**: 체크포인트로 웨이브가 일시정지(`WaveSystem.isPaused()`)된 동안 필드에 남아있는 적을 클릭/스킬로 공격할 수 있던 문제를 고쳐, `Game.handleCellClick`/`castUltimate` 둘 다 정비 중에는 아무 반응도 하지 않게 가드를 추가했다.
- **카드 크기 확대**: 보드 위 적/아군/플레이어 카드(`entity-card`)를 68px/80px→80px/96px로, 손패 카드를 64×88px→72×96px(정확히 3:4)로 키워 Unmelting 플레이어 카드(`clamp(150,17vw,200) × 3/4`) 비율의 세로로 긴 느낌에 더 가깝게 만들었다. 커진 카드에 맞춰 그리드 칸 내 좌우 대치 오프셋(`GRID_ROLE_OFFSET_X` 34→40px)과 본진 칸 적 오프셋(`BOSS_ENEMY_OFFSET_X` 44→52px), 플레이어 카드 좌측 이동폭(`--board-figure-shift-x` -50→-60px)도 비례 조정해 겹침 없이 유지했다.

## 2026-07-03 (13) — 웨이브 30초 주기 + 클리어 즉시 다음 웨이브 + 스킬 발동 방식 정리
- `WAVE_PUSH_INTERVAL_MS`를 3분→30초로 단축. `WaveSystem`에 `pushTimeoutId`를 두고 `damageEnemy`에서 처치 직후 그리드+본진에 살아있는 적이 하나도 없으면(`isBoardClear`) 대기 중인 푸시 타이머를 즉시 취소하고 `handlePushTimeout`을 그 자리에서 실행해 다음 웨이브가 30초를 다 기다리지 않고 바로 밀려오게 했다(`triggerInstantPushIfClear`). 체크포인트로 정비 중(`paused`)일 때는 발동하지 않는다.
- 스킬 발동 방식 변경: 기본 공격(코스트1)은 더 이상 스킬 오르브를 먼저 클릭해 "조준"할 필요 없이 보드 칸을 클릭하는 즉시 무조건 발동한다. 스킬(코스트10, 전체 타격)은 기존과 동일하게 스킬 아이콘을 직접 클릭해야만 발동한다.
- `AbilitySystem`에서 `basicArmed`/`isBasicArmed`/`toggleArmBasic`/`disarmBasic`을 전부 제거(코스트 보유 여부만 판단). `SkillBar`의 기본 공격 오르브는 클릭 리스너 없는 순수 코스트 표시로 남고 `SkillBarHandlers`도 `onUltimateClick`만 남았다.
- `BoardRenderer.setTargeting`/`is-targeting`/`is-targetable` 관련 CSS·메서드를 제거하고, 기본 공격에 조준 단계가 없어진 만큼 모든 칸(그리드+본진)이 항상 클릭 가능하도록 커서/호버 하이라이트를 상시 적용으로 전환했다. 배치 모드(`is-placement-targeting`)는 기존과 동일하게 카드 선택 시에만 켜진다.
- 더 이상 쓰이지 않게 된 `HandSystem.clearSelection()`, `.skill-orb.is-armed` 펄스 애니메이션도 함께 제거.
- Playwright로 확인: 오르브를 누르지 않고 칸을 바로 클릭하면 코스트가 즉시 차감되며 공격이 나가고, 기본 오르브를 눌러도 아무 일도 일어나지 않는 것(코스트 불변, 타겟팅 클래스 없음), 3칸을 모두 처치하면 30초를 기다리지 않고 곧바로 다음 웨이브가 밀려오는 것(처치 후 약 0.15초 만에 웨이브 번호 증가)까지 확인.

## 2026-07-03 (12) — 동일 칸 조우 전투 + 돌진 클래시 이펙트 + 적 입장 슬라이드
- 전투 발생 조건 변경: `WaveSystem.stepEnemy`가 더 이상 인접 접근만으로 이동을 막지 않고, 적은 아군이 있는 칸에도 자유롭게 걸어 들어간다. 실제로 같은 칸(또는 본진)에 적과 아군이 함께 있을 때만 `resolveClashes`가 매 틱 서로 피해를 교환하고, 그동안 그 칸의 적은 배회를 멈춘다(`stepEnemy` 조기 반환).
- `WaveSystem`에 `onClash` 이벤트 추가(`ClashInfo{cellIndex}`), 클래시가 발생한 칸을 매 틱 UI에 통지한다.
- `BoardRenderer.upsertToken`을 `style.transform` 직접 대입 대신 `--token-x`/`--token-y` CSS 커스텀 프로퍼티로 전환(`board.css` `.board-token`도 `transform: translate(var(--token-x), var(--token-y))`로 변경)해, 슬라이드 트랜지션을 깨지 않고 위에 얹을 수 있는 별도 애니메이션 레이어를 만들었다.
- 신규 `BoardRenderer.playClashFx(cellIndex)`: 해당 칸(또는 본진) 큐 맨 앞의 적/아군 토큰에 `is-lunging` 클래스 + `--token-lunge-x`를 부여해 서로에게 짧게 돌진했다 되돌아오는 `board-token-lunge` 키프레임(0.28s)을 재생한다. `Game.handleClash`가 `onClash`를 받아 `playClashFx` 호출 + 충돌 지점에 `BubbleBurst.burstAt`(옅게 터지는 얕은 버블)을 동시에 띄운다.
- `BubbleBurst`에 `burstAt(x, y, opts)` 추가: 기존 `travelTo`(이동형)와 별개로 한 지점에서 사방으로 짧게 튀는 정지형 팝 이펙트(count/size/spread 옵션), 클래시 착지 연출 전용.
- 적 신규 스폰 시 그리드 진입 열(우측 끝)보다 한 칸 더 바깥(가상 칸, `CELL_SIZE+CELL_GAP`만큼 우측)에서 시작해 다음 프레임에 실제 칸 좌표로 갱신 — 기존 슬라이드 트랜지션이 그대로 "밖에서 걸어 들어오는" 연출을 만든다(`upsertToken`의 `spawnFromX`, 적 역할에만 적용, 아군/본진 개체는 기존과 동일).
- Playwright로 확인: 기본 공격으로 카드 획득 → 적이 있는 칸에 배치 → 다음 틱에 `is-lunging`/`bubble-burst-piece` 동시 발생(본진 칸 포함), 새 웨이브 진입 시 적 3기가 그리드 바깥 위치에서 시작해 입구 열로 슬라이드해 들어오는 것.

## 2026-07-03 (11) — 칸당 다중 배치 + 좌우 대치 구도 + 이펙트 정리
- `WaveSystem.cells`/`DefenderSystem.cells`를 `EnemyToken | null`/`AllyToken | null` 배열에서 `EnemyToken[]`/`AllyToken[]` 배열로 전환. 적은 한 칸에 무제한으로 쌓일 수 있고(입구 강제 푸시도 더 이상 점유 여부로 스킵하지 않음), 아군은 칸당 최대 3기까지 배치 가능(`DefenderSystem` MAX_ALLIES_PER_CELL=3).
- 칸 내부 배치 규칙 추가: 아군은 칸 중심에서 살짝 좌측, 적은 살짝 우측에 서도록 `BoardRenderer`가 역할별 x오프셋(±34px)과 다중 개체 시 y축 스택 오프셋을 계산해 픽셀 단위로 배치— 마주 선 대치 구도가 실제로 보인다.
- 적이 그리드를 완전히 벗어나 본진에 도달해도 더 이상 즉시 처치 판정되지 않고 `WaveSystem.bossEnemies` 큐에 들어가 플레이어 칸 안에서 실제로 서 있는 위협이 된다. 플레이어 칸도 이제 `BOSS_CELL_INDEX(-1)` 셀로 취급되어 공격 조준/배치 타깃이 모두 가능(플레이어 카드는 좌측으로 살짝 밀려나 자리를 만들고, 아군은 중앙, 적은 우측에 선다 — 좌: 플레이어 · 중: 아군 · 우: 적).
- `applyDamage`/클래시 로직을 칸(또는 본진) 큐의 맨 앞 개체 기준으로 재작성(`damageEnemy` 헬퍼)해 다중 스택에서도 처치·카드/아이템 드롭·체크포인트 파이프라인이 동일하게 동작한다.
- 체력바 애니메이션(`hp-water-flow` 흐르는 효과) 제거, 정적인 차분한 청색 그라디언트로 교체.
- `BubbleBurst`를 언멜팅 `SquareBurst` 스타일(그라디언트/보더/글로우 없는 단색 4색 팔레트, 여러 개 겹쳐서 층을 이루는 플랫한 느낌)로 재작업.
- 신규 `src/systems/BoardConstants.ts`에 `BOSS_CELL_INDEX` 상수를 두어 WaveSystem/DefenderSystem/BoardRenderer/Game이 본진 셀을 그리드 셀과 동일한 방식으로 다룬다.

## 2026-07-03 (10) — 강제 웨이브 푸시 + 체크포인트(소강상태) + 진행 버튼
- 웨이브 진행 방식을 킬-클리어 기반에서 **타이머 강제 진행**으로 전면 교체했다. `WaveSystem`이 3분마다 `pushReinforcements()`를 호출해 엔트리 열이 비어있는 칸에만 적을 3마리까지 추가한다(이미 서있는 스트래글러는 덮어쓰지 않고 다음 푸시까지 남는다) — 처치를 못 해도 계속 밀려오는 실제 압박이 생겼다.
- 5회 푸시마다 체크포인트: `WaveSystem.pause()`로 이동/타이머를 멈추고 `onCheckpoint` 이벤트를 쏜다. 이때는 화면을 가리지 않아 배치·공격 등 "정비"는 계속 가능하다.
- 체크포인트 3번 중 2번은 보드판 아래 세련된 원형 "진행" 버튼(`ProceedButton`)이 깜빡이며 뜨고, 누르면 `resumeFromCheckpoint()`로 다음 5웨이브가 다시 시작된다.
- 3번째 체크포인트(9,10번 로그의 3웨이브-단위 유물과 달리 이제는 **체크포인트 3회, 즉 15웨이브마다**)는 진행 버튼 대신 기존 유물 보상 화면이 뜨고, 유물을 고르는 행동 자체가 진행 버튼을 대신해 다음 웨이브를 재개한다.
- 이에 따라 `EncounterResult.relicAwarded`와 킬-클리어 기반 웨이브/유물 카운팅 로직은 전부 제거 — 처치는 이제 카드/아이템 드롭 판정만 담당하고, 웨이브·유물 진행은 순수히 체크포인트 시스템이 담당한다.
- 웨이브 HUD는 일시정지 중엔 "웨이브 N - 정비 중"으로 바뀐다.
- 3분 타이머를 그대로 기다리기 어려워 로컬에서 상수를 임시로 낮춰(2초/체크포인트당 2웨이브) 체크포인트→진행 버튼→재개→유물 체크포인트→선택까지 Playwright로 전 과정을 확인한 뒤 실제 값(3분/5웨이브/체크포인트 3회)으로 되돌렸다.

## 2026-07-03 (9) — 적/아군 슬라이드 이동 + 웨이브 타이머 HUD
- `BoardRenderer`를 토큰-아이덴티티 기반으로 재구성: 적/아군 카드는 더 이상 `.board-cell`에 다시 그려 넣는 방식(칸 이동 시 순간이동처럼 보임)이 아니라, entity id로 지속되는 `.board-token` 엘리먼트를 `.board-grid` 위에 절대좌표(px)로 얹어 `transform`만 갱신한다. 같은 DOM 노드가 유지되므로 CSS `transition: transform 0.85s`가 실제로 칸-칸 이동을 스르륵 보간한다. `.board-cell`은 이제 클릭 타깃/점유 발광만 담당하는 순수 바닥 소켓.
- 부작용: 카드가 board-cell의 자식이 아니게 되면서 `has-enemy:hover` 호버 리프트 연출은 제거됨(추후 필요하면 JS로 재구현).
- 상단 중앙에 `웨이브 N - MM:SS` HUD(`WaveHud`) 추가. `WaveSystem`이 웨이브 시작 시각을 기록해 3분 카운트다운을 노출한다 — 지금은 표시용이며 웨이브 진행 자체는 기존처럼 3마리를 모두 처치해야 다음 웨이브가 뜬다(타이머가 0에 도달해도 강제로 다음 웨이브를 밀어넣진 않음). "3분마다 강제로 웨이브가 밀려오는" 것까지 원하시면 별도로 로직을 바꿔야 한다.

## 2026-07-03 (8) — 유물/인벤토리 패널 분리 + 소모품 드롭
- 우측 하단 단일 "유물" 패널을 `.side-panels` 컨테이너로 감싸 상단 유물 / 하단 인벤토리 2개 패널로 분리했다(`RelicInventory`는 그대로, `ItemInventory` 신설).
- 적 처치 시 35% 확률로 소모품 1개가 인벤토리로 드롭된다(`ItemSystem`) — 카드 획득(50%)과는 독립적인 별도 확률 롤이며, 같은 처치에서 둘 다 뜨면 손패/인벤토리로 각각 다른 버블이 동시에 날아간다.
- `src/data/ConsumablePool.ts`: 유물과 마찬가지로 실제 효과는 없는 이름만 있는 placeholder 8종에서 무작위 1개를 뽑는다.
- 인벤토리 슬롯은 유물(보라)과 구분되게 민트색으로 채워지며, 아이콘도 새로 만든 물약(vial) 아이콘을 사용한다.

## 2026-07-03 (7) — 3웨이브 유물 보상 화면
- 3웨이브 클리어 시 유물 1개를 바로 지급하던 것을, 화면이 어두워지며 3장 중 1장을 고르는 보상 화면(`RewardOverlay`)으로 교체했다. 오버레이가 전체 화면을 덮어 클릭을 자연히 차단하고, `WaveSystem.pause()/resume()`로 적 이동/웨이브 스폰도 실제로 멈춘다.
- 고른 카드 위치에서 유물 인벤토리(`RelicInventory.getDropPoint()`)까지 `BubbleBurst`가 날아가 도착 시 실제로 인벤토리에 추가된다(도착 전에는 상태 변경 없음).
- `src/data/RelicPool.ts`: 실제 유물 효과는 아직 없어 이름만 있는 8종 placeholder 풀에서 매번 3개를 중복 없이 뽑는다 — 3장이 서로 달라 보이는 데 필요한 최소한의 콘텐츠.
- Playwright로 웨이브 3개 클리어 → 오버레이 등장(보드/손패/스킬바 어두워지며 정지) → 1장 선택 → 버블 비행 → 인벤토리 슬롯 채워짐까지 전 과정 확인.

## 2026-07-03 (6) — 디펜더 배치 시스템
- 손패 카드를 클릭하면 선택(부채꼴에서 위로 들림) → 보드 빈 칸을 클릭하면 배치되는 흐름을 추가. 배치 중엔 그리드 전체에 주황색 테두리(공격 조준의 보라색과 구분)가 뜬다. 기본 공격 조준과 카드 선택은 상호 배타적으로 서로를 해제한다(`AbilitySystem.disarmBasic()` / `HandSystem.clearSelection()`).
- `DefenderSystem` 신설: 칸당 아군 1기, hp(기본 6) 보유. `WaveSystem`은 `DefenderHooks`(getHp/damage) 인터페이스로만 접근해 두 시스템이 서로를 직접 참조하지 않는다.
- 적이 아군이 있는 칸으로 이동을 시도하면 더 이상 통과하지 못하고 그 자리에서 서로 피해를 교환한다(적→아군 1, 아군→적 2, 매 틱). 적이 죽으면 기존 처치 파이프라인(카드 획득/웨이브·유물)을 그대로 탄다.
- 아군 카드는 `EntityCard`의 새 `ally` variant로 렌더링 — 적과 동일한 실루엣 아이콘을 냉기(청록)에서 온기(주황)로만 recolor해 "사령됨"을 표현했다(`CONCEPT_ART.md`의 before/after 색 규칙을 실제로 적용한 첫 사례).
- Playwright로 카드 획득→선택→빈 칸 배치→아군 카드 렌더→적이 배회하다 부딪혀 서로 체력이 줄어드는 것까지 확인.
- 미완료: 배치된 아군의 개별 패시브/공격 사거리, 진화(3합성), 보스 사령, 도감.

## 2026-07-03 (5) — 풀 일러스트 카드 + 심해 체력바
- `EntityCard`(`src/ui/EntityCard.ts`) 신설: 적/플레이어(추후 아군)가 공유하는 풀 일러스트 스타일 카드. 프레임 레이아웃(인셋 하이라이트/하단 인셋 그림자/외곽 섀도우·글로우, 하단만 어두워지는 오버레이, 하단 고정 콘텐츠)은 Unmelting `player-card`/`hp-bar` 구조를 그대로 참고했다.
- 실제 일러스트 이미지는 아직 없음 — 기존 flat 아이콘(해골/해파리)을 카드 전면에 반투명 워터마크 실루엣으로 깔아 "풀 일러스트" 자리만 잡아뒀다. 추후 진짜 원화로 교체 시 `.entity-card-art` 배경만 바꾸면 된다.
- 체력바는 숫자 표기 없이 하단에 고정, 청록~밝은 하늘색 그라디언트가 흐르는 애니메이션(`hp-water-flow`)으로 "심해 물" 느낌을 냈다. 너비(`width: hp/maxHp*100%`)는 Unmelting `hp-fill`과 동일하게 width% + transition 방식.
- `EnemyToken`에 `maxHp` 추가, 적 카드 체력바가 실제 hp 비율을 반영해 공격 맞을 때마다 줄어드는 것까지 Playwright로 확인.
- 플레이어 카드의 체력바는 아직 와이어링할 본진 체력 시스템이 없어 항상 100%로 고정된 자리만 확보(TBD 유지).

## 2026-07-03 (4) — 보드 확대 + 저주 공격 스킬 2종
- 보드를 대폭 확대(셀 84→140px, 본진 칸 96→160px)하고 퍼스펙티브/틸트 값을 그 크기에 맞게 재조정(`perspective` 560→950px, `--board-tilt` 66→62deg)해 가독성을 유지했다.
- 좌측 하단에 원형 스킬 오르브 2개 추가: 기본 공격(코스트1, 클릭 셀 단일 타격)과 스킬(코스트10, 필드 전체 동시 타격). `AbilitySystem`이 코스트 풀(최대10, 1.2초당 +1 회복)과 기본 공격 조준 상태를 관리한다.
- 보드 셀 클릭의 의미가 바뀌었다: 더 이상 클릭만으로 즉시 처치되지 않고, 기본 공격을 조준(오르브 클릭)한 뒤 셀을 클릭해야 발동한다. 조준 중에는 그리드 전체에 보라색 타겟팅 테두리가 뜬다.
- `CurseMortar` 이펙트 추가: 플레이어 카드에서 곡선(박격포형 고점 아치)으로 발사되어 대상 칸에 착탄, 착탄 순간에만 데미지가 실제로 적용되도록 `onImpact` 콜백으로 시각/로직을 동기화했다. 착탄 지점에 `FloatingDamage`로 "-2"류 숫자가 떠오른다.
- `EnemyToken`에 `hp`(기본 4) 추가, `WaveSystem.applyDamage()`로 기본 공격(2데미지)·스킬(4데미지, 필드 전체)이 동일한 처치/카드획득/웨이브·유물 규칙을 타도록 연결.
- 체력바는 아직 없음(요청대로 적 카드 데이터가 생기면 언멜팅처럼 카드에 내장 예정) — 지금은 착탄 시 데미지 숫자만 표시.
- Playwright로 조준→발사→착탄→데미지 숫자, 스킬 발사 시 여러 발 동시 발사(±지연 스태거)까지 확인.

## 2026-07-03 (3) — 보드 3D 기울임 + 적 이동/조우
- `board.css`: `.board-layout`에 `perspective`(560px)+`rotateX(66deg)`를 적용해 3×3 그리드+본진 칸만 바닥처럼 눕혔다. 손패/유물 인벤토리는 평면 그대로 유지(요청대로 보드만 기울임).
- 타일 위 아이콘/라벨은 `.board-figure` 래퍼에서 반대 각도로 `rotateX(calc(var(--board-tilt) * -1))`+`translateZ`로 되돌려 세워둔 스탠딩 카드처럼 보이게 하는 빌보드 기법 적용.
- `WaveSystem` 전면 재작성: 적은 이제 보스방 반대쪽인 그리드 우측 열(진입 지점)에서만 스폰되고, 1.1초 틱마다 보스방 방향으로 전진하거나 위/아래로 배회한다. 셀 클릭(수동 처치)과 보스방 도착(자동 조우) 둘 다 동일한 `resolveEncounter`(50% 카드 획득 등)로 처리 — `onEncounter` 이벤트로 UI에 통지.
- 보스방에 적이 도달하면 본진 칸에 붉은 경고 발광 펄스(`is-engaged`)가 뜬다. 아직 본진 체력/패배 조건은 없음(추가 시스템 필요, TBD로 유지).
- Playwright로 방치 시 적이 실제로 배회하다 보스방에 자동 도달해 카드를 드롭하는 것과, 수동 클릭 시 버블 이펙트가 여전히 정상 작동하는 것을 확인.
- 미완료: 본진 체력/패배 조건, 진화(3합성), 보스 사령, 도감 비포/애프터.

## 2026-07-03 (2) — 메인 화면 UI 1차 구현
- 화면 구성 확정: 중앙 대형 보드판(좌측 네크로맨서 본진 1칸 + 흐름 화살표 + 우측 3×3 적 그리드), 하단 전체 폭 손패(부채꼴), 우측 하단 유물 인벤토리(반투명 검은 그리드).
- 처치 → 50% 확률로 손패에 카드 획득 → 3웨이브 클리어마다 유물 획득 규칙을 `WaveSystem`으로 최초 구현(적 40종 데이터가 붙기 전까지는 플레이스홀더 토큰 사용, 규칙 자체는 실제 동작).
- `BubbleBurst` 이펙트 추가: Unmelting `SquareBurst`와 달리 사각 산개가 아니라 반투명 원형 버블이 처치 지점 → 손패 다음 슬롯으로 유영하듯 이동해 도착 시 카드가 팝인.
- 손패 부채꼴 배치는 Unmelting 유물 팬(`--relic-x/--relic-y/--relic-rot` CSS 변수 스택)과 같은 문법을 손패용으로 재구현(`--hand-x/--hand-y/--hand-rot`).
- 팔레트를 심연 남보라/인광 청록 톤으로 확정(레퍼런스: 사용자 제공 Abyss-Necro 컨셉 이미지)하고 `board.css`에 CSS 변수로 정리.
- Playwright로 개발 서버 렌더 확인: 보드 클릭 → 셀 소멸 애니메이션 → 버블 유영 → 손패 팬 추가 → 웨이브 클리어 시 재스폰까지 실제 동작 확인.
- 미완료: 진화(3합성), 보스 사령, 도감 비포/애프터, 실제 40종 데이터 연결.

## 2026-07-03
- 프로젝트 컨셉 확정: 탑뷰 디펜스, 네크로맨서가 처치한 심연 좀비/물고기를 사령해 디펜더로 전환하는 코어 루프.
- 넥슨 재밌넥 시험 제출 목적/3일 일정/클라이맥스 버티컬 슬라이스 방향을 `CLAUDE.md`, `Abyss_Necro_Game_Concept.md`에 문서화.
- **포트폴리오 핵심 메시지 확정**: "AI로 만든 캐릭터 수 자랑"이 아니라 "AI 파이프라인이 있어야 가능한 콘텐츠 스케일(적 40 / 사령 카드 40 / 진화체 20 / 보스 5)"을 보여주는 것이 핵심 차별점.
- **코어 루프 확정**: 적 처치 → 사령 카드 획득(1:1, 40종) → 동일 카드 3장 합성 → 진화체(20종) 전환 → 보스도 동일 메카닉으로 사령(5종) → 도감에서 적(before)/사령(after) 비교. 카드별 고유 패시브 1개.
- 우선순위 결정: 카드 획득/합성/보스 사령/도감 비포·애프터 시스템이 1순위(포트폴리오 차별점), 인게임 디펜스 웨이브/전투는 이 시스템을 보여주는 무대로서 2순위 폴리싱.
- 아트 방향(팔레트/캐릭터 실루엣 언어/before-after 색 대비 규칙/Unmelting 레퍼런스 범위)을 `CONCEPT_ART.md`로 분리 기록.
- TypeScript + Vite + Electron 스캐폴드 구성: 렌더러(Vite)와 Electron main/preload 분리, electron-builder Windows(nsis/portable) 패키징 설정 추가, 플레이스홀더 보드 렌더로 파이프라인 동작 확인.
- 에셋 포맷 방침: Unmelting 실사례(스프라이트 215개 전부 webp) 근거로 최종 일러스트는 WebP, PNG는 아이콘 변환 등 예외 용도로만 사용하기로 결정.
- 미완료: 카드/진화/사령/도감 시스템 구현, 40+40+20+5 명단·패시브 확정, 실제 캐릭터 원화, 아이콘 세트, 사운드.
